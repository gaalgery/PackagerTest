<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.packager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Packager">
        <activity android:name=".userAccount.fragments.ssActivity"></activity>
        <activity
            android:name=".userAccount.UserAccountActivity"
            android:label="@string/title_activity_user_account"
            android:theme="@style/Theme.Packager.NoActionBar" />
        <activity
            android:name=".packageChooser.PackageListActivity"
            android:label="@string/title_activity_package_list"
            android:theme="@style/Theme.Packager.NoActionBar" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>